put photon into flash mode:
  1. Hold mode button
  2. press rst button
  3. wait till flashes yellow

compile photon code
  1. cd to {SPARKCORE_DIRECTORY}/firmware/modules
  2. run `make all program-dfu PLATFORM=photon APP={APPLICATION_NAME} APPDIR={APPLICATION_DIR}`
